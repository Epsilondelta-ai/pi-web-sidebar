# Plugin CSS Restoration

## Re:ZERO Tasks

1. Restore plugin-owned styling for the pi-web sidebar plugin and verify it visually in Playwright.
   - Done: `bun run build` output renders at http://localhost:4321/ with usable self-contained plugin styling; checks pass; changes committed.
   - Depends on: none
   - Parallel: no
